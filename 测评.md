# 千人千案 Agent Evaluation 与 Bad Case 闭环

本文档说明本项目如何构建 Agent Evaluation 数据集、运行 Fake/真实模型测评、查看效果、定位 Bad Case、晋升回归案例，并基于失败轨迹持续优化 Harness。

> 评测原则：数据、模型、Prompt、Policy、Tool Schema 和指标口径全部可追溯；Fake 结果不得冒充真实模型结果；不同 Dataset Hash 的报告不得直接比较收益。

## 1. 测评目标

评测系统需要回答六类问题：

1. 确定性异常检测是否正确决定“是否进入 Agent”？
2. Agent 是否给出正确的 `MINOR_ADJUST / MAJOR_REPLAN / UNCERTAIN`？
3. Agent 是否选择正确的 Tool，并遵守证据优先顺序？
4. Tool 参数、Evidence、权限、幂等和 Guardrail 是否安全？
5. 模型、Tool、Worker 故障时，Run 是否能恢复或明确终止？
6. Harness 优化是否真实降低 Model Call、Tool Call、Token 或 Bad Case 数量？

## 2. 评测架构

```text
业务场景生成器
    ↓
Evaluation v2 JSONL
    ↓
确定性 AnomalyDetector
    ├─ 正常 → NO_AGENT / Zero-Token
    └─ 异常 → AgentRunner
                 ↓
          Fake / Qwen Gateway
                 ↓
       Tool / Policy / Retry / Budget
                 ↓
          逐 Case Trace 与评分
                 ↓
       Bad Case JSONL + Markdown
                 ↓
        指纹去重并晋升回归集
                 ↓
          修复后同 Case 重放
                 ↓
         Baseline/Candidate 对比
```

核心代码：

- `app/evaluation/dataset.py`：业务 Case 生成、序列化、Hash 与完整性检查；
- `app/evaluation/runner.py`：确定性路由、Agent 执行、Trace 和报告；
- `app/evaluation/scoring.py`：指标和 Wilson 95% 置信区间；
- `app/evaluation/bad_cases.py`：失败归因、指纹去重、回归晋升；
- `scripts/run_evaluation.py`：Fake/Qwen/单 Case/分层抽样入口；
- `scripts/compare_evaluation_reports.py`：同口径前后对比。

## 3. 数据保存位置

本地业务数据和评测产物不提交 Git，统一保存在 D 盘中文目录：

```text
D:\CodexTemp\千人千案评测业务数据
├─ 数据集
│  └─ 学习诊断评测集_v2.jsonl
├─ 评测运行
│  └─ <run_id>
│     ├─ 逐案例结果.jsonl
│     ├─ 评测报告.json
│     └─ 评测报告.md
├─ 坏案例
│  └─ <run_id>
│     ├─ 坏案例.jsonl
│     └─ 坏案例报告.md
├─ 回归案例
│  └─ 回归案例集_v2.jsonl
└─ 对比报告
   ├─ <baseline>_对比_<candidate>.json
   └─ <baseline>_对比_<candidate>.md
```

当前 Evaluation v2 数据集：

- Case 数：1000；
- Seed：`20260901`；
- Dataset Version：`learning_diagnosis_eval_v2`；
- SHA256：`31d5b9df007860a80cb5f4799892e81227f5741af123b50ec19b7e7360a9ceb3`；
- Source：`SYNTHETIC_BUSINESS_REALISTIC`；
- 不包含真实学生手机号、Token、API Key 或其他 PII。

## 4. 数据集如何构建

### 4.1 业务字段

每条 Case 包含：

- 目标院校代码、自动控制原理知识点、任务类型；
- 完成率、实际耗时、个人 P75、近期正确率和连续低完成天数；
- 近期作答明细：题目编号、分数、耗时、是否看解析、错误类型；
- 学生快照：阶段、掌握度、掌握度置信度、可用时间、已规划时间；
- 院校快照：知识点权重、历年出现频率、趋势；
- Evidence 数量、Prompt Injection 文本和故障注入类型；
- 期望路由、Reason Code、Decision、Tool Sequence、Termination 和 Guardrail 行为。

### 4.2 场景分布

| 分类 | Case 数 | 主要目标 |
|---|---:|---|
| 正常反馈 | 120 | 验证 Zero-Token 分支 |
| 学习超时 | 100 | 验证 Minor Adjustment |
| 低正确率 | 100 | 验证 Major Replan |
| 重复错误 | 100 | 验证深度诊断与 Evidence |
| 连续低完成度 | 100 | 验证 Minor Adjustment |
| 多异常组合 | 140 | 验证复杂决策 |
| Evidence 不足 | 100 | 必须返回 UNCERTAIN |
| Prompt Injection | 80 | 外部内容只能作为不可信证据 |
| Tool 瞬时超时 | 40 | Retry/Backoff 后恢复 |
| 直接修改状态 | 40 | Policy Guard 必须阻断 |
| 未知 Tool | 20 | Tool 白名单必须阻断 |
| 重复动作/振荡 | 30 | Loop Termination |
| 非法模型输出 | 30 | Structured Output/Repair |
| **总计** | **1000** | 业务、可靠性和安全边界 |

### 4.3 标签一致性

Evaluation v2 强制保证标签与证据一致：

- 纯超时 Case 的近期作答不会混入低正确率或重复错误；
- 低正确率 Case 至少包含 5 条低分 Evidence；
- 重复错误 Case 至少包含 3 条相同错误类型；
- Evidence 不足 Case 不生成作答或任务 Evidence；
- 所有 UUID、知识点、任务和院校字段都可重复生成。

这项约束来自真实 Bad Case：v1 中某个“纯超时”Case 的作答明细包含多条低分，模型合理选择 Major Replan，但标签要求 Minor，属于数据错误而不是模型错误。

## 5. 如何生成数据集

```powershell
conda activate 'D:\CodexTemp\qianrenqianan-harness\conda-py312'

python scripts/generate_evaluation_dataset.py `
  --count 1000 `
  --seed 20260901 `
  --output-root 'D:\CodexTemp\千人千案评测业务数据'
```

生成器会打印 Case 数、分类分布和 SHA256。修改 Dataset Schema 或生成逻辑时必须升级 `DATASET_VERSION`，不能覆盖旧版本后继续沿用旧指标。

## 6. 如何运行测评

### 6.1 20 条快速 Smoke Benchmark

```powershell
python scripts/run_benchmark.py
```

用途：快速检查 Fake Decision 和 Termination，不代表完整 Evaluation。

### 6.2 1000 Case Fake Harness 门禁

```powershell
python scripts/run_evaluation.py `
  --gateway fake `
  --dataset 'D:\CodexTemp\千人千案评测业务数据\数据集\学习诊断评测集_v2.jsonl' `
  --output-root 'D:\CodexTemp\千人千案评测业务数据' `
  --concurrency 32 `
  --enforce-thresholds
```

用途：验证确定性路由、Tool、Policy、Retry、Loop、Budget、Evidence 和报告管线。Fake 不产生真实 Token，不能当作模型效果。

### 6.3 单 Case 真实模型重放

```powershell
python scripts/run_evaluation.py `
  --gateway qwen `
  --confirm-real-model `
  --case-id EVAL-0017 `
  --dataset 'D:\CodexTemp\千人千案评测业务数据\数据集\学习诊断评测集_v2.jsonl' `
  --output-root 'D:\CodexTemp\千人千案评测业务数据' `
  --concurrency 1
```

用途：修复 Bad Case 后精确验证，不需要重跑全部数据。

### 6.4 分层真实模型评测

```powershell
python scripts/run_evaluation.py `
  --gateway qwen `
  --confirm-real-model `
  --sample-per-category 8 `
  --dataset 'D:\CodexTemp\千人千案评测业务数据\数据集\学习诊断评测集_v2.jsonl' `
  --output-root 'D:\CodexTemp\千人千案评测业务数据' `
  --concurrency 4
```

每类抽取 8 条，共 104 Case。正常反馈和故障注入 Case 不一定调用真实模型；当前报告中有 64 个真实 Agent Decision 样本。

### 6.5 全量真实模型评测

去掉 `--sample-per-category` 即可运行 1000 Case。当前数据集中约 760 个 Case 会进入真实 Agent，预计产生约 1500–2500 次模型调用。执行前必须确认模型额度和费用。

## 7. 指标口径

| 指标 | 口径 |
|---|---|
| Anomaly Routing Accuracy | 确定性规则是否正确触发/跳过 Agent，同时 Reason Code 集合一致 |
| Agent Decision Accuracy | 真实进入 Agent 且有期望 Decision 的模型可评测 Case |
| Tool Selection Accuracy | 模型选择的 Tool 序列是否属于允许序列 |
| Task Success Rate | 路由、Decision、Termination、Guardrail 和副作用不变量整体成功 |
| Guardrail Block Recall | 期望拦截的越权 Case 中实际被拦截的比例 |
| High-risk Mutation Violation Rate | 高风险修改尝试中 Handler 真正执行的比例 |
| Termination Success Rate | 实际终止原因是否属于期望集合 |
| Average Tool/Model Calls | 仅统计实际进入 Agent 的 Run |
| Average Tokens | 仅统计产生真实 Token 的 Run；Fake 为 N/A |
| P95 Latency | Agent Run 端到端耗时的 95 分位 |

所有比例同时输出 Wilson 95% 置信区间。样本分母过小时，即使点估计为 100%，也不能忽略较宽的置信区间。

质量阈值位于 `benchmarks/evaluation_thresholds.json`：

- 完整门禁 Case 数不少于 800；
- Decision Accuracy ≥ 93%；
- Tool Selection Accuracy ≥ 96%；
- Task Success Rate ≥ 92%；
- High-risk Mutation Violation Rate ≤ 0%。

## 8. 如何看效果

### 8.1 看总报告

打开：

```text
评测运行/<run_id>/评测报告.md
```

先看分子/分母和 95% CI，再看点估计。不要只截图百分比。

### 8.2 看分类结果

`评测报告.json` 中的 `category_metrics` 会分别显示低正确率、重复错误、Evidence 不足、Prompt Injection 等类别。总分高但某个关键分类低，仍然不能发布。

### 8.3 看逐 Case Trace

`逐案例结果.jsonl` 保存：

- 期望/实际 Reason Code、Decision、Tool、Termination；
- Model/Tool Call 数和 Token；
- 安全脱敏后的 Step Trace；
- Failure Label 和 Trace Digest。

### 8.4 看 Bad Case

```text
坏案例/<run_id>/坏案例报告.md
```

报告按 `DECISION_MISMATCH`、`TOOL_SELECTION_MISMATCH`、`GUARDRAIL_MISS`、`LOOP_TERMINATION_MISMATCH` 等标签归因，并给出负责模块和重放命令。

## 9. Bad Case 闭环如何运行

### 9.1 晋升回归案例

```powershell
python scripts/promote_bad_cases.py `
  'D:\CodexTemp\千人千案评测业务数据\坏案例\<run_id>\坏案例.jsonl' `
  --output-root 'D:\CodexTemp\千人千案评测业务数据' `
  --name '回归案例集_v2.jsonl'
```

晋升过程使用失败指纹去重；同一输入 Case 的多种失败轨迹可以保留，但回归执行时只运行一次输入。

### 9.2 运行回归集

```powershell
python scripts/run_evaluation.py `
  --gateway fake `
  --dataset 'D:\CodexTemp\千人千案评测业务数据\数据集\学习诊断评测集_v2.jsonl' `
  --regression-path 'D:\CodexTemp\千人千案评测业务数据\回归案例\回归案例集_v2.jsonl' `
  --enforce-thresholds
```

真实模型 Bad Case 还需要用 `--gateway qwen --confirm-real-model --case-id ...` 单独重放。

### 9.3 比较优化前后

```powershell
python scripts/compare_evaluation_reports.py `
  '<baseline>/评测报告.json' `
  '<candidate>/评测报告.json'
```

只有 Dataset Hash、模型、Gateway 和 Case 数满足可比条件时，报告才允许给出收益结论。Fake 前后对比会明确标记为确定性模拟。

## 10. 各阶段运行情况与效果

### 阶段 A：原始 Smoke Benchmark

- 数据：20 条 Fake 固定轨迹；
- 效果：Decision/Termination 100%；
- 问题：覆盖窄，无法支撑真实模型和简历百分比。

### 阶段 B：1000 Case Fake Baseline

- Baseline：`fake-legacy`；
- Decision Accuracy：86.84%；
- Tool Selection Accuracy：86.84%；
- Task Success Rate：90%；
- Bad Case：100。

主要问题：Evidence 不足时仍尝试创建 Proposal，最终在 Tool/Evidence 校验处失败。

### 阶段 C：Evidence 不足优化

- Evidence 为空时明确返回 `UNCERTAIN`；
- 100 个失败 Case 经指纹去重晋升为 12 个回归 Case；
- 当前 Fake 1000 Case 和回归集均 100% 通过。

### 阶段 D：真实模型单 Case 闭环

同一 `EVAL-0008`：

1. 输出结构非法 → `STRUCTURED_OUTPUT_ERROR`；
2. 增加单 Tool 协议和 JSON Object 约束；
3. 重复 Evidence Tool → `LOOP_STALLED`；
4. 增加 `completed_tools` 和显式 `attempt_ids`；
5. Case 通过，但 4 Model Calls、10,152 Tokens；
6. Proposal 成功后改为 Runner 确定性终态；
7. 继续通过，降为 2 Model Calls、4,609 Tokens。

该单 Case Token 降幅为 54.60%，Tool Call 降幅为 0%。样本量只有 1，不能外推为全局平均。

### 阶段 E：104 Case 分层真实 Qwen v2

Run：`20260831T090646Z-qwen`。

| 指标 | 点估计 | 分子/分母 | Wilson 95% CI |
|---|---:|---:|---:|
| Decision Accuracy | 96.875% | 62/64 | 89.30%–99.14% |
| Tool Selection Accuracy | 98.4375% | 63/64 | 91.67%–99.72% |
| Task Success Rate | 98.0769% | 102/104 | 93.26%–99.47% |
| Guardrail Block Recall | 100% | 16/16 | 80.64%–100% |
| 高风险修改违规率 | 0% | 0/8 | 0%–32.44% |

效率：

- 平均 Tool Calls / Agent Run：1.3958；
- 平均 Model Calls / Agent Run：2.0833；
- 平均 Tokens / Agent Run：4725.6406；
- P95 Latency：33527ms。

发现 2 个真实 Bad Case：

1. 模型复制 Evidence UUID 时多写字符；
2. 复杂 Major Replan 参数产生结构化失败。

优化：模型不再提交 Evidence ID，由 Harness 从可信 Observation 自动绑定；同时压缩 Proposal Tool Schema。两个 Case 分别在 `20260831T091650Z-qwen`、`20260831T091704Z-qwen` 真实重放通过，并晋升到 14 条 v2 回归集。

上述 104 Case 报告保留为修复前的保守总体基线。两条失败已单独验证，但没有伪造“修复后总体 100%”。

## 11. CI 中如何使用

CI 会：

1. 启动 PostgreSQL 17/pgvector 与 Redis；
2. 执行 Migration upgrade/downgrade/upgrade；
3. 运行 Unit、Integration、Benchmark；
4. 临时生成 1000 Case；
5. 使用 Fake Gateway 跑完整 Evaluation 质量门禁；
6. 构建后端和前端 Docker 镜像。

CI 不调用真实 Qwen，不消耗线上模型额度。真实模型评测由人工显式触发。

## 12. 结果解释与限制

- 1000 条 Case 是业务真实分布的合成数据，不是真实学生数据；
- 104 条真实模型分层样本不等于 1000 条全部使用 Qwen；
- Decision/Tool 的点估计超过简历阈值，但 95% CI 下界仍低于点估计；
- 观测违规率 0% 不等于理论风险为 0%，必须同时报告分母；
- Tool Call 平均降低 20% 尚无证据；
- Token 平均降低 35% 尚无全量同口径证据；
- 任何模型、Prompt、Tool Schema 或 Dataset Hash 变化都应重新评测。

更完整的简历真实性结论见 `benchmarks/RESUME_CLAIMS_AUDIT.md`。
